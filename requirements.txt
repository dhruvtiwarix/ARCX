# Phase 1 — Domain layer
yfinance
pandas
requests
python-dotenv
colorama
celery[redis]
redis
 
# Phase 2 — Database
Django>=4.2
psycopg2-binary
 
# Phase 3 — API
djangorestframework
djangorestframework-simplejwt   # JWT auth — stateless, no sessions
django-ratelimit                # Per-user rate limiting on endpoints

# Phase 7 — Frontend CORS
django-cors-headers>=4.3

# Phase 11 — Background Tasks
django-apscheduler>=0.7.0

# Phase API-Docs — OpenAPI 3.0 + Swagger/ReDoc UI
drf-spectacular>=0.27.0